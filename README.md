# نظام إدارة الصيدلية | Pharmacy Management System

نظام ويب لإدارة الصيدليات مبني على Laravel.

---

## نظرة عامة

المشروع يتيح للصيدليات إدارة المنتجات والطلبات والفواتير والموردين والمستخدمين مع واجهة نقطة بيع (POS).

---

## المتطلبات

| المتطلب | الإصدار |
|---------|----------|
| PHP | 7.3 أو 8.0 |
| MySQL | - |
| Composer | - |
| Node.js | للواجهة الأمامية |

---

## التقنيات المستخدمة

- **Backend:** Laravel 8
- **قاعدة البيانات:** MySQL
- **الواجهة:** Blade، Bootstrap 4
- **أدوات:** DataTables، Select2، Chart.js

---

## الميزات الرئيسية

### 🔐 المصادقة
- تسجيل الدخول والتسجيل
- إعادة تعيين كلمة المرور
- أدوار المستخدمين (Admin، Cashier، Manager)

### 🛒 نقطة البيع (POS)
- إنشاء الطلبات بسرعة
- إضافة المنتجات والكميات والخصومات
- طباعة الفواتير

### 📦 إدارة المنتجات
- إضافة وتعديل وحذف المنتجات
- رفع صور المنتجات
- تنبيه عند نقص المخزون
- المورد وسعر الشراء وسعر البيع

### 📋 إدارة الطلبات
- عرض الطلبات والفواتير
- تفاصيل كل طلب

### 👥 إدارة المستخدمين
- إضافة وحذف المستخدمين
- الصلاحيات حسب الدور

### 📊 التقارير
- عرض التقارير والفواتير

### 🏢 إضافات أخرى
- إدارة الموردين
- إدارة الشركات
- المعاملات المالية

---

## هيكل المشروع

```
pharmacy/
├── app/
│   ├── Http/Controllers/    # المتحكمات
│   └── Models/              # النماذج (Product, Order, User...)
├── config/                  # إعدادات التطبيق
├── database/migrations/     # جداول قاعدة البيانات
├── resources/views/         # صفحات الويب
├── routes/web.php          # المسارات
└── public/                 # الملفات العامة
```

---

## التثبيت

1. **نسخ ملف البيئة:**
   ```bash
   cp .env.example .env
   ```

2. **توليد مفتاح التطبيق:**
   ```bash
   php artisan key:generate
   ```

3. **تعديل إعدادات قاعدة البيانات في `.env`:**
   ```
   DB_DATABASE=pharmacy
   DB_USERNAME=root
   DB_PASSWORD=
   ```

4. **تشغيل الهجرات:**
   ```bash
   php artisan migrate
   ```

5. **تثبيت الحزم:**
   ```bash
   composer install
   npm install && npm run dev
   ```

6. **تشغيل السيرفر:**
   ```bash
   php artisan serve
   ```

ثم افتح المتصفح على: `http://localhost:8000`

---

## المسارات الرئيسية

| المسار | الوظيفة |
|-------|---------|
| `/` | صفحة تسجيل الدخول |
| `/dashboard` | لوحة التحكم |
| `/orders` | الطلبات |
| `/products` | المنتجات |
| `/users` | المستخدمين |
| `/suppliers` | الموردين |
| `/report` | التقارير |

---

## قاعدة البيانات

- **users** - المستخدمون
- **orders** - الطلبات
- **order_details** - تفاصيل الطلبات (المنتجات في كل طلب)
- **products** - المنتجات
- **suppliers** - الموردون
- **companies** - الشركات
- **transactions** - المعاملات المالية

---

## الترخيص

MIT License
